enclave {
    from "sgx_tstdc.edl" import *;

    trusted {
        /* define ECALLs here. */
        // int xpos = 80 + (100 * (seqNumber % 5))
        public void calculate_xpos(int32_t seqNumber, [out] int32_t* xpos);
    };

    untrusted {
        /* define OCALLs here. */

    };
};
